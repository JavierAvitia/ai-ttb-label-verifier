# ai-ttb-label-verifier
AI-powered label verification tool for TTB compliance agents. Upload label images (batch supported), OCR extracts text via EasyOCR, then fuzzy/exact matching validates brand, ABV, warning statement, and 4 more fields against application data. Color-coded results, severity-sorted, with confidence scores. Built with Streamlit. Deployed, fast, simple.
